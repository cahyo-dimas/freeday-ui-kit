# .NET build outputs — never ship in the npm tarball (the RCL is consumed as source via
# <ProjectReference>, and these are regenerated by `dotnet build`). Scoped here so the root
# .gitignore fallback keeps applying everywhere else.
bin/
obj/
