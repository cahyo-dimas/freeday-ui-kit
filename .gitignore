# OS
.DS_Store
Thumbs.db

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Editor
.vscode/
.idea/
*.swp

# Local agent/session state (scheduler locks, SDD scratch, etc.)
.claude/
.superpowers/

# NOTE: dist/ is intentionally committed so projects can import
# freeday.tokens.css / freeday.css directly without running a build.
